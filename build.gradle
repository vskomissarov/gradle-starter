plugins {
    id 'java'
}

apply from: 'script.gradle'
apply plugin: CustomPlugin.class



//buildSrc --- id 'custom '
class CustomPlugin implements Plugin<Project> {

    @Override
    void apply(Project project) {
        addHelloTask(project)
    }

    void addHelloTask(Project project) {
        def map = [description: "Hello task", group: "Liga", type: DefaultTask]
        project.task("hello") {
            println "configuration phase Hello task"

            doLast {
                println "execution phase, Action1"
            }
            doFirst {
                println "execution phase, Action2"
            }
            doLast {
                println "execution phase, Action3"
            }

            println "task $description, group $group, actions: ${actions.size()}"
            println "path task: $path"
        }
    }
}
